# Streamlit theme + server config (used locally and on Streamlit Community Cloud).
[theme]
primaryColor = "#e63946"
backgroundColor = "#ffffff"
secondaryBackgroundColor = "#f1faee"
textColor = "#1d3557"
font = "sans serif"

[server]
headless = true
enableCORS = false

[browser]
gatherUsageStats = false
